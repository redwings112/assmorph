import React from 'react';
import { AppRegistry } from 'react-native';
import Home from '../../expo-template-default/app/screens/Home';

// Register the app component directly with the app name as a string
AppRegistry.registerComponent('ass morph', () => Home);